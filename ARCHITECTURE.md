# 📊 PROJECT ARCHITECTURE & COMPONENTS

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    USER LAYER                              │
├─────────────────────────────────────────────────────────────┤
│  Command Line  │  Web Interface  │  REST API  │  Docker    │
└────────┬────────────────────────┬────────────────────────┬──┘
         │                        │                        │
┌────────▼────────────────────────▼────────────────────────▼──┐
│                    APPLICATION LAYER                       │
├─────────────────────────────────────────────────────────────┤
│  main.py       │  app.py (FastAPI) │  config.py             │
│  Training      │  REST API         │  Configuration         │
│  Prediction    │  Web Service      │  Settings              │
└────────┬────────────────────────┬────────────────────────┬──┘
         │                        │                        │
┌────────▼────────────────────────▼────────────────────────▼──┐
│                    CORE ML LAYER                          │
├─────────────────────────────────────────────────────────────┤
│  AutomatedMLModel                                          │
│  ├─ Data Loading        ├─ Preprocessing   ├─ Training    │
│  ├─ Data Analysis       ├─ Encoding        ├─ Prediction  │
│  └─ Validation          └─ Scaling         └─ Evaluation  │
└────────┬────────────────────────┬────────────────────────┬──┘
         │                        │                        │
┌────────▼────────────────────────▼────────────────────────▼──┐
│                    UTILITIES LAYER                         │
├─────────────────────────────────────────────────────────────┤
│  logger.py     │  utils.py        │  train.py              │
│  Logging       │  CSV Operations  │  Model Training        │
│  Monitoring    │  Data Utils      │  Evaluation            │
└────────┬────────────────────────┬────────────────────────┬──┘
         │                        │                        │
┌────────▼────────────────────────▼────────────────────────▼──┐
│                    DATA LAYER                             │
├─────────────────────────────────────────────────────────────┤
│  data/          │  models/        │  output/   │  logs/   │
│  CSV Files      │  Saved Models   │  Results   │  Logs    │
└─────────────────────────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT LAYER                       │
├─────────────────────────────────────────────────────────────┤
│
│  Option 1: Local Development
│  ┌────────────────────────────────────┐
│  │ Your Computer                      │
│  │ ├─ Python Environment              │
│  │ ├─ FastAPI App (port 5000)         │
│  │ └─ Local Data Files                │
│  └────────────────────────────────────┘
│
│  Option 2: Docker Container
│  ┌────────────────────────────────────┐
│  │ Docker Container                   │
│  │ ├─ Python 3.12 Image               │
│  │ ├─ Flask App                       │
│  │ ├─ All Dependencies                │
│  │ └─ Mounted Volumes                 │
│  └────────────────────────────────────┘
│
│  Option 3: Docker Compose (Multi-Container)
│  ┌──────────────────────────────────────┐
│  │ docker-compose.yml                   │
│  │ ├─ ml-api service                    │
│  │ ├─ Port mapping (5000:5000)          │
│  │ ├─ Volume persistence                │
│  │ └─ Health checks                     │
│  └──────────────────────────────────────┘
│
│  Option 4: Cloud Deployment
│  ┌──────────────────────────────────────┐
│  │ AWS/GCP/Heroku/Azure                │
│  │ ├─ Gunicorn + Nginx                  │
│  │ ├─ Load Balancing                    │
│  │ ├─ Auto-scaling                      │
│  │ └─ Monitoring & Logging              │
│  └──────────────────────────────────────┘
│
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
Training Flow:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ CSV File     │───>│ Load & Parse │───>│ Analyze Data │
│ train.csv    │    │              │    │              │
└──────────────┘    └──────────────┘    └──────┬───────┘
                                                │
                         ┌──────────────────────▼────────────────┐
                         │ Preprocess Data                       │
                         │ ├─ Handle Missing Values              │
                         │ ├─ Encode Categories                  │
                         │ └─ Scale Features                     │
                         └──────────────────┬─────────────────────┘
                                            │
                         ┌──────────────────▼────────────────┐
                         │ Train Model                       │
                         │ ├─ Split Train/Test               │
                         │ ├─ Fit Model                      │
                         │ └─ Evaluate Performance           │
                         └──────────────────┬─────────────────┘
                                            │
                    ┌───────────────────────▼──────────────────┐
                    │                                          │
             ┌──────▼──────┐                         ┌─────────▼──────┐
             │ Model.pkl   │                         │ report.txt     │
             │ (Saved)     │                         │ (Metrics)      │
             └─────────────┘                         └────────────────┘


Prediction Flow:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ CSV File     │───>│ Load & Parse │───>│ Preprocess   │
│ test.csv     │    │              │    │ (using saved │
└──────────────┘    └──────────────┘    │ scalers)     │
                                         └──────┬───────┘
                                                │
                         ┌──────────────────────▼────────────────┐
                         │ Load Trained Model                    │
                         │ model.pkl                             │
                         └──────────────────┬─────────────────────┘
                                            │
                         ┌──────────────────▼────────────────┐
                         │ Make Predictions                  │
                         │ ├─ Predict Classes                │
                         │ ├─ Calculate Probabilities        │
                         │ └─ Confidence Scores              │
                         └──────────────────┬─────────────────┘
                                            │
                    ┌───────────────────────▼──────────────────┐
                    │                                          │
             ┌──────▼──────┐                     ┌────────────▼─┐
             │predictions. │                     │ summary.txt  │
             │ csv         │                     │ (Report)     │
             └─────────────┘                     └──────────────┘
```

---

## API Request/Response Flow

```
Client Request
     │
     ▼
┌─────────────────────────────────────┐
│ HTTP Request (POST /predict)        │
│ Content: CSV File                   │
└──────────────┬──────────────────────┘
               │
               ▼
        ┌─────────────────────┐
        │ FastAPI App (app.py) │
        └──────────┬──────────┘
                   │
       ┌───────────┼───────────┐
       │           │           │
       ▼           ▼           ▼
   ┌─────┐    ┌────────┐  ┌─────────┐
   │File │    │Validate│  │Load Data│
   │Check│    │CSV     │  │         │
   └──┬──┘    └────┬───┘  └────┬────┘
      │            │            │
      └────────┬───┴────────────┘
               │
               ▼
        ┌──────────────────┐
        │Load Model        │
        │model.pkl         │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │Make Predictions  │
        │AutomatedMLModel  │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │Save Results      │
        │predictions.csv   │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │JSON Response     │
        │+ Download Link   │
        └────────┬─────────┘
                 │
                 ▼
        Client Response (JSON)
```

---

## File Structure & Dependencies

```
ML CSV Automation/
│
├── [Core Files]
│   ├── main.py                    (Entry point - Command line)
│   ├── app.py                     (FastAPI API)
│   ├── config.py                  (Configuration)
│   └── setup.bat                  (Setup automation)
│
├── [ML Model]
│   ├── models/
│   │   ├── autom_model.py         (Automated ML class)
│   │   ├── model.pkl              (Trained model - saved)
│   │   └── ...
│   │
│   ├── src/                       (Source code)
│   │   ├── __init__.py
│   │   ├── train.py               (Training logic)
│   │   ├── predict.py             (Prediction logic)
│   │   ├── utils.py               (Utilities)
│   │   └── logger.py              (Logging)
│   │
│   └── data/                      (Input data)
│       ├── train.csv              (Training data)
│       ├── test.csv               (Test data)
│       └── demo.csv               (Sample data)
│
├── [Deployment]
│   ├── Dockerfile                 (Docker container config)
│   ├── docker-compose.yml         (Docker orchestration)
│   ├── .env.example               (Environment template)
│   └── requirements.txt           (Python dependencies)
│
├── [Documentation]
│   ├── README.md                  (Project overview)
│   ├── API_DOCUMENTATION.md       (API reference)
│   ├── DEPLOYMENT_GUIDE.md        (Deployment instructions)
│   ├── DEPLOYMENT_CHECKLIST.md    (Pre-deploy checklist)
│   ├── PRODUCTION_READY.md        (Quick start guide)
│   ├── SETUP_COMPLETE.md          (Setup summary)
│   └── PROJECT_COMPLETE.md        (Project stats)
│
├── [Testing]
│   ├── tests.py                   (Unit tests)
│   └── __pycache__/               (Python cache)
│
├── [Output]
│   ├── output/                    (Results)
│   │   ├── predictions.csv        (Predictions)
│   │   └── report.txt             (Reports)
│   │
│   └── logs/                      (Logs)
│       └── execution.log          (Execution logs)
│
└── [Other]
    ├── .gitignore                 (Git ignore)
    ├── notebooks/                 (Jupyter notebooks)
    └── configs/                   (Additional configs)
```

---

## Technology Stack

```
┌─────────────────────────────────────────┐
│         TECHNOLOGY STACK                │
├─────────────────────────────────────────┤
│
│ Programming Language:
│ └─ Python 3.8+
│
│ Web Framework:
│ └─ FastAPI 0.95+ (REST API)
│
│ ML Libraries:
│ ├─ scikit-learn 1.0+ (Models)
│ ├─ pandas (Data manipulation)
│ └─ numpy (Numerical computing)
│
│ Deployment:
│ ├─ Docker (Containerization)
│ ├─ Docker Compose (Orchestration)
│ ├─ Gunicorn (WSGI Server)
│ └─ Nginx (Load Balancer - optional)
│
│ Testing:
│ └─ unittest (Built-in Python)
│
│ Logging:
│ └─ Python logging module
│
│ Visualization:
│ ├─ matplotlib
│ └─ seaborn
│
│ Notebooks:
│ └─ Jupyter
│
│ Configuration:
│ └─ python-dotenv (.env files)
│
└─────────────────────────────────────────┘
```

---

## Development vs Production

```
DEVELOPMENT                         PRODUCTION
┌──────────────────────┐            ┌──────────────────────┐
│ Local Machine        │            │ Cloud Server         │
│ Python 3.12          │            │ Python 3.12 (Docker) │
│ FastAPI (dev server) │            │ Uvicorn (ASGI)       │
│ SQLite (no DB)       │            │ Nginx (Reverse Proxy)│
│ Single core          │            │ Multi-core           │
│ Manual testing       │            │ Automated tests      │
│ Debug=True           │            │ Debug=False          │
│ Limited logging      │            │ Comprehensive logging│
│ No monitoring        │            │ Full monitoring      │
│ port:5000            │            │ port:80/443          │
└──────────────────────┘            └──────────────────────┘

Development Workflow:
1. Code → 2. Test Local → 3. Git Commit → 4. Push

Production Workflow:
1. Pull Code → 2. Build Docker → 3. Run Tests → 4. Deploy
5. Health Checks → 6. Monitor → 7. Alert if issues
```

---

## Security Architecture

```
┌─────────────────────────────────────────────┐
│              SECURITY LAYERS                │
├─────────────────────────────────────────────┤
│
│ 1. Network Layer (Production)
│    ├─ SSL/TLS Encryption (HTTPS)
│    ├─ Firewall Rules
│    └─ Rate Limiting
│
│ 2. Application Layer
│    ├─ API Key Authentication
│    ├─ Input Validation
│    └─ File Upload Validation
│
│ 3. Data Layer
│    ├─ Secure File Storage
│    ├─ Model Encryption
│    └─ Secure Credential Storage (.env)
│
│ 4. Logging Layer
│    ├─ Error Logging
│    ├─ Audit Trail
│    └─ No Sensitive Data in Logs
│
└─────────────────────────────────────────────┘
```

---

## Monitoring & Observability

```
┌──────────────────────────────────────────┐
│     MONITORING & OBSERVABILITY           │
├──────────────────────────────────────────┤
│
│ Application Metrics:
│ ├─ API Response Time
│ ├─ Request Count
│ ├─ Error Rate
│ └─ Model Accuracy
│
│ System Metrics:
│ ├─ CPU Usage
│ ├─ Memory Usage
│ ├─ Disk Space
│ └─ Network I/O
│
│ Logging:
│ ├─ Application Logs
│ ├─ Request Logs
│ ├─ Error Logs
│ └─ Audit Logs
│
│ Health Checks:
│ ├─ /health endpoint
│ ├─ Model availability
│ ├─ Disk space check
│ └─ Database connectivity
│
│ Alerting:
│ ├─ High error rate
│ ├─ Memory threshold
│ ├─ Slow response time
│ └─ Service down
│
└──────────────────────────────────────────┘
```

---

This diagram shows how all components work together to create a production-ready ML automation system!
